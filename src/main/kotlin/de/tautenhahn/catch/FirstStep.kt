// package works like in Java, but there is no package private and directory is not checked
package de.tautenhahn.catch

/**
Just a Hello World example. Note that the main function must be outside class.
 */
fun main(args: Array<String>): Unit {
	val msg: String = "Hello TAU"
	println(msg)
}